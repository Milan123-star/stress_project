data_ingestion:
  test_size: 0.2

feature_engineering:
  encoder: OneHotEncoder

model_building:
  param_grid:
    max_depth: [4, 6, 8]
    learning_rate: [0.01, 0.05, 0.1]
    n_estimators: [100, 200]